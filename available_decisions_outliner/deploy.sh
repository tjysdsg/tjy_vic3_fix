. ../path.sh

cp -r .metadata "${GAME_PATH}"/mod/available_decisions_outliner/ || exit 1
cp -r gui "${GAME_PATH}"/mod/available_decisions_outliner/ || exit 1
cp thumbnail.png "${GAME_PATH}"/mod/available_decisions_outliner/ || exit 1
